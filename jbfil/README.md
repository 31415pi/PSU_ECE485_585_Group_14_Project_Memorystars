
# Makefile Use

```sh
make                    # Run with defaule input.
make inpfile=bob        # Run with custom input.
make inpfile="a file"   # Run with custom input.
make show_read=1        # Show each line read.
```

